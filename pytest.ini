[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: tests that are network-dependent or long-running (deselect with '-m "not slow"')
    integration: tests that exercise multiple components together
addopts = -ra
